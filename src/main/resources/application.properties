spring.application.name=Ticket_Management_System_

#if you want to use h2 database, uncomment the following lines and comment out the postgresql lines

#h2 -------------------start here---------------------- 

spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

spring.h2.console.enabled=true

#h2 ----------------end here----------------------------------------

#and comment out the postgresql lines below

# cloud db --------------------start here----------------------

# spring.datasource.url=${DB_URL}
# spring.datasource.username=${DB_USERNAME}
# spring.datasource.password=${DB_PASSWORD}

# spring.datasource.driver-class-name=org.postgresql.Driver

# spring.jpa.hibernate.ddl-auto=update

# #new
# spring.jpa.show-sql=false

# logging.level.org.hibernate.SQL=OFF
# logging.level.org.hibernate.orm.jdbc.bind=OFF

# spring.jpa.properties.hibernate.default_batch_fetch_size=50
# spring.jpa.properties.hibernate.jdbc.batch_size=50

# spring.datasource.hikari.minimum-idle=5
# spring.datasource.hikari.maximum-pool-size=20
# spring.datasource.hikari.connection-timeout=3000
# spring.datasource.hikari.keepalive-time=300000
# spring.datasource.hikari.max-lifetime=1800000

# #----
# spring.h2.console.enabled=false



#cloud db --------------------end here----------------------

spring.h2.console.path=/h2-console

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=sdna.proj@gmail.com
spring.mail.password=hjqr vjpd ydmg arpx
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
jwt.secret=put-a-very-long-random-secret-here-at-least-32-characters
jwt.expiration-ms=7200000
vaadin.devmode.enabled=false

# ── WSEP external systems (V3 — Bucket A) ────────────────────────────────────
# Endpoint + timeouts for the WSEP 2026 payment / ticket-issuance service.
# wsep.enabled=true is the production default: real HTTP calls go to WSEP.
# Tests override this to "false" in src/test/resources/application.properties
# so they use the deterministic in-memory stubs (no network in CI).
wsep.base-url=https://damp-lynna-wsep-1984852e.koyeb.app/
wsep.connect-timeout-ms=3000
wsep.read-timeout-ms=10000
wsep.enabled=true


app.initial-state.enabled=true
app.initial-state.file=classpath:initial-state.json


